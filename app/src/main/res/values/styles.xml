<resources>

    <!-- Base application theme. -->
    <!-- Dark theme -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorControlHighlight">#686F77</item>
        <!-- Hamburger icon/navigation up icon colors in toolbar -->
        <item name="colorControlNormal">#FFFFFF</item>
        <!-- Custom attrs -->
        <item name="toolBarColor">@color/colorPrimary</item>
        <item name="textColor">#FFFFFF</item>
        <item name="lightTextColor">#DBDFE3</item>
        <item name="veryLightTextColor">#989EA5</item>
        <item name="backgroundColor">#28323F</item>
        <item name="borderColor">#898D93</item>
        <item name="buttonColor">@color/colorAccent</item>

        <!-- Action mode stuff! -->
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/Widget.App.ActionMode</item>
        <item name="actionModeCloseDrawable">@drawable/ic_close_24dp</item>
        <item name="actionBarTheme">@style/ThemeOverlay.MaterialComponents.Dark.ActionBar</item>
    </style>

    <style name="SplashScreen" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <!-- Action mode stuff! -->
    <style name="Widget.App.ActionMode" parent="Widget.AppCompat.ActionMode">
        <item name="titleTextStyle">?attr/textAppearanceHeadline6</item>
        <item name="subtitleTextStyle">?attr/textAppearanceSubtitle1</item>
        <item name="background">@color/darkSnackBarColor</item>
    </style>

    <!-- Light theme -->
    <style name="LightTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/lightThemeBackground</item>
        <item name="colorPrimaryDark">#000000</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorControlHighlight">#C0CCDC</item>
        <!-- Hamburger icon/navigation up icon colors in toolbar -->
        <item name="colorControlNormal">@color/colorPrimaryDark</item>
        <!-- Custom attrs -->
        <item name="toolBarColor">@color/lightThemeBackground</item>
        <item name="textColor">#000000</item>
        <item name="lightTextColor">#3A3A40</item>
        <item name="veryLightTextColor">#6D6D81</item>
        <item name="backgroundColor">@color/lightThemeBackground</item>
        <item name="borderColor">#8C939C</item>
        <item name="buttonColor">@color/colorAccent</item>

        <!-- Action mode stuff! -->
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/Widget.App.ActionMode</item>
        <item name="actionModeCloseDrawable">@drawable/ic_close_24dp</item>
        <item name="actionBarTheme">@style/ThemeOverlay.MaterialComponents.Dark.ActionBar</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="settings_text_group_title">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginTop">24dp</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>
